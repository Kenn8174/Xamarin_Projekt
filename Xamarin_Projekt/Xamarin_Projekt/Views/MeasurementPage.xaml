<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:behavior="clr-namespace:Xamarin_Projekt.Behaviors"
             x:Class="Xamarin_Projekt.Views.MeasurementPage"
             Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout>
            <!-- Temperatur -->
            <Label Text="Temperatur" FontSize="Medium" TextColor="Black"/>
            <Entry Text="{Binding Temperatur}" Placeholder="Temperatur">
                <Entry.Behaviors>
                    <behavior:NumberCheckBehavior/>
                </Entry.Behaviors>
            </Entry>
            <!-- Fugtighed -->
            <Label Text="Humidity" FontSize="Medium" TextColor="Black"/>
            <Entry Text="{Binding Humidity}" Placeholder="Humidity">
                <Entry.Behaviors>
                    <behavior:NumberCheckBehavior/>
                </Entry.Behaviors>
            </Entry>
            <!-- Hent data fra API til appen -->
            <Button Text="Hent" FontSize="Medium" Command="{Binding GetMeasurementCommand}" IsEnabled="{Binding IsValid}"/>
            <!-- Tilføj data fra appen til API -->
            <Button Text="Tilføj" FontSize="Medium" Command="{Binding PostMeasurementCommand}" IsEnabled="{Binding IsValid}"/>
            <ActivityIndicator IsRunning="{Binding IsBusy}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>